/**
  Mobile Header
  Global header styles for mobile
  @fileinfo
  */

/**
  Menu
  Add the class 'menuBar' to column group containing the topMenu widget in Content Studio.
  @group structure
  @style
  .menuBar Sets the background of the menu to #333
  */

.menuBar {
    background: #333;
    border-top: 1px solid #fff;
}

#page .mobileHeader {
    background:#000;
    display:none;
    -webkit-box-shadow:0 0 10px #000;
    box-shadow:0 0 10px #000;
    position:fixed;
    left:0;
    right:0;
    bottom:0;
    z-index:1000;
}

/**
  Scrubber
  A scrubber menu
  @style
  .mobileHeader.scrubber Basic styles for menubar
  */

#page .mobileHeader.scrubber {
    clear:both;
    background: #333;
    -webkit-box-shadow:0 2px 6px rgba(0,0,0,.5) inset;
    box-shadow:0 2px 6px rgba(0,0,0,.5) inset;
    display:block;
    overflow: hidden;
    position:relative;
    top:auto;
    right:auto;
    bottom:auto;
    left:auto;
    width:100%;
}

/**
  Scrubber arrows
  Inserts arrows and gradients when menu is scrolled
  @style
  .mobileHeader.scrubber:before Left arrow
  .mobileHeader.scrubber:before Right arrow
  */

#page .mobileHeader.scrubber:before,
#page .mobileHeader.scrubber:after {
    content: '';
    color:#fff;
    display:block;
    position:absolute;
    top:0;
    z-index:100;
    visibility:visible;
    width:20px;
    height:100%;
    line-height: 42px;
}

#page .mobileHeader.scrubber.isLeft:before, #page .mobileHeader.scrubber.isRight:after {
    display:none;
}

#page .mobileHeader.scrubber:before {
    content: "\25c4";
    background-image: -moz-linear-gradient(left, rgba(0,0,0,1) 20%, rgba(0,0,0,0));
    background-image: -webkit-gradient(linear, left top, right top, color-stop(0.2, rgba(0,0,0,1)), color-stop(1,rgba(0,0,0,0)));
    background-image: -webkit-linear-gradient(left, rgba(0,0,0,1 20%), rgba(0,0,0,0));
    background-image: linear-gradient(left, rgba(0,0,0,1) 20%, rgba(0,0,0,0));
    left:0;
    text-align: left;
}

#page .mobileHeader.scrubber:after {
    content: "\25ba";
    background-image: -moz-linear-gradient(left, rgba(0,0,0,0), rgba(0,0,0,1) 80%);
    background-image: -webkit-gradient(linear, left top, right top, color-stop(.8, rgba(0,0,0,1)), color-stop(0,rgba(0,0,0,0)));
    background-image: -webkit-linear-gradient(left, rgba(0,0,0,0), rgba(0,0,0,1) 80%);
    background-image: linear-gradient(left, rgba(0,0,0,0), rgba(0,0,0,1) 80%);
    right:0;
    text-align: right;
}

/* Currently not in use*/
.mobileHeader .handle {
    background: #000;
    border: 1px solid #fff;
    border-bottom: 0 none;
    border-radius:5px 5px 0 0;
    -webkit-box-shadow:0 0 10px #000;
    box-shadow:0 0 10px #000;
    cursor:pointer;
    font-size:18px;
    color:#fff;
    line-height:50px;
    text-align:center;
    margin-left:-50px;
    position:absolute;
    top:-51px;
    left:50%;
    z-index:2;
    width:100px;
    height:50px;
}

.mobileHeader.scrubber .handle {
    display:none;
}