<header>Visning</header>
{{if visiting}}
<ul class="features">
    {{each visiting}}
    <li>
        {{if viewingDate}}<strong>${viewingDate}</strong>: ${viewingTimeFrom}&ndash;${viewingTimeTo}{{/if}}
        ${viewingNote}
    </li>
    {{/each}}
</ul>
{{else}}
<ul class="features">
    <li>Vennligst ta kontakt med megler, for &aring; gj&oslash;re avtale om privat visning.</li>
</ul>
{{/if}}