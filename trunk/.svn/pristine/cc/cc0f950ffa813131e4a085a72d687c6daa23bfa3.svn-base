mno.core.register({
    id:'widget.tvGuide.recommendation',
    creator:function (sandbox) {
        var $ = sandbox.$;
        function init() {
            if(sandbox.container){
                sandbox.container.each(function(i, element){
                    var $element = $(element);
                });
            }
        }

        function destroy() {}

        return {
            init:init,
            destroy:destroy
        }
    }
});