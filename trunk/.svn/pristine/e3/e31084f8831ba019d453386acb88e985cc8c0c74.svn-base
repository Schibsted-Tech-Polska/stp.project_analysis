mno.core.register({
    id:'widget.cartoon.section',
    extend:['widget.slideshow.main'],
    creator: function (sandbox) {
        function init() {
            if(sandbox.container){
                this.slideshow(sandbox.container);
            }
        }

        function destroy() {
        }

        return {
            init:init,
            destroy:destroy
        }
    }
});