mno.core.register({
    extend:['widget.pageTools.main'],
    id:'widget.pageTools.addThisPlain',
    creator: function (sandbox) {
        function init() {
            this.loadAddThis(sandbox);
        }

        function destroy() {}

        return {
            init:init,
            destroy:destroy
        }
    }
});